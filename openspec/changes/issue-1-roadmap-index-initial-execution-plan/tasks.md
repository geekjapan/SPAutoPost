## 1. OpenSpec change

- [x] 1.1 Create the `roadmap-index` capability proposal and spec.
- [x] 1.2 Keep the change docs-only and scoped to Issue #1 acceptance evidence.

## 2. Roadmap evidence

- [x] 2.1 Record that M0-M6 GitHub milestones exist.
- [x] 2.2 Record that M0-M6 issues are assigned to milestones.
- [x] 2.3 Record the agreed initial implementation order source.
- [x] 2.4 Record the OpenSpec-first agent rule sources.

## 3. Verification and PR

- [x] 3.1 Run `openspec validate issue-1-roadmap-index-initial-execution-plan --strict`.
- [x] 3.2 Run `openspec validate --changes --strict`.
- [x] 3.3 Run `git diff --check`.
- [x] 3.4 Create a conventional commit, push the branch, and open a PR.
